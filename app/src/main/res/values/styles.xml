<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="NavigationItemTitleBold">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#808080</item> <!-- Adjust color if needed -->
        <item name="android:background">#D3D3D3</item> <!-- Light gray -->

    </style>
    <!-- Splash Theme for the initial screen -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/durian2</item>
    </style>
    <!-- Base application theme
    <style name="ButtonTheme" parent="ThemeOverlay.AppCompat">
        <item name="buttonStyle">@style/CustomButtonStyle</item>
    </style>

  Custom style for buttons
    <style name="CustomButtonStyle">
        <item name="android:backgroundTint">#fff06e</item>
        <item name="android:textColor">#000000</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    -->
</resources>
